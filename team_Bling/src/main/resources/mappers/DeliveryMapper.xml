<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.project.bling.mapper.deliveryMapper">

	<select id="deivery_list" parameterType="int" resultType="CombineVO">
	<![CDATA[
		select * from order_list WHERE midx=#{midx} AND send_day >= to_char(sysdate-90) and rnum =1
		ORDER BY order_idx ASC, detail_idx ASC
	]]>
	</select>
	  
</mapper>